Comprehensions demasiado grandes
→ usar un for puede ser más legible.

Usar comprehension solo para print/side effects
→ mejor for.

zip() con tamaños diferentes
→ se detiene en la colección más corta.

any() y all()
→ trabajan con truthiness / condiciones booleanas.

min([]) / max([])
→ ValueError.

sort()
→ modifica y devuelve None.

sorted()
→ devuelve lista nueva.

lambda demasiado compleja
→ mejor def.

dict comprehension con claves repetidas
→ valores anteriores pueden sobrescribirse.